# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Instructions:
# 1. Create a Supabase project at https://supabase.com
# 2. Copy this file to .env
# 3. Replace the values above with your actual Supabase credentials
# 4. Create the following tables in Supabase:
#
#    contact_submissions:
#      - id (uuid, primary key, default: uuid_generate_v4())
#      - name (text)
#      - email (text)
#      - phone (text, nullable)
#      - message (text)
#      - created_at (timestamp, default: now())
#
#    gallery_images:
#      - id (uuid, primary key, default: uuid_generate_v4())
#      - url (text)
#      - title (text, nullable)
#      - description (text, nullable)
#      - created_at (timestamp, default: now())
#
# 5. Create a storage bucket named 'gallery' for image uploads
# 6. Set up Row Level Security (RLS) policies as needed
